import React from 'react';

interface Props {
}

const WeaponTable: React.FC<Props> = ({ }) => {

    return (
        <div>
            WeaponTable
        </div>
    );
};

export default WeaponTable;
